diff --git a/style.css b/style.css
index ecb0b51..bf852d6 100644
--- a/style.css
+++ b/style.css
@@ -86,23 +86,23 @@ section {
   position: sticky;
   top: 0;
   z-index: 100;
-  background: rgba(247, 243, 234, 0.82);
+  background: rgba(10, 21, 38, 0.95);
   backdrop-filter: blur(18px);
   padding: 16px 30px;
   display: flex;
   justify-content: space-between;
   align-items: center;
   gap: 20px;
-  transition: all 0.35s ease;
-  box-shadow: 0 14px 32px rgba(17, 34, 64, 0.08);
-  border-bottom: 1px solid rgba(17, 34, 64, 0.08);
+  transition: all 0.35s ease, background 0.3s ease, border-color 0.3s ease;
+  box-shadow: 0 14px 32px rgba(0, 0, 0, 0.2);
+  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
 }
 
 .header.shrink {
   padding-top: 12px;
   padding-bottom: 12px;
-  background: rgba(247, 243, 234, 0.94);
-  box-shadow: 0 18px 36px rgba(17, 34, 64, 0.12);
+  background: rgba(10, 21, 38, 0.98);
+  box-shadow: 0 18px 36px rgba(0, 0, 0, 0.25);
 }
 
 .logo {
@@ -111,6 +111,12 @@ section {
   gap: 14px;
   text-decoration: none;
   flex-shrink: 0;
+  transition: transform 0.3s ease;
+}
+
+.logo:hover {
+  transform: translateY(-2px);
+  filter: drop-shadow(0 4px 12px rgba(195, 163, 98, 0.3));
 }
 
 .logo img {
@@ -151,6 +157,10 @@ section {
   color: rgba(17, 34, 64, 0.56);
 }
 
+.header .brand-eyebrow {
+  color: rgba(255, 255, 255, 0.6);
+}
+
 .brand-name {
   font-family: "Playfair Display", serif;
   font-size: 25px;
@@ -160,6 +170,26 @@ section {
   color: var(--emerald-deep);
 }
 
+.header .brand-name {
+  background: linear-gradient(
+    110deg,
+    #ffffff 30%,
+    var(--champagne) 50%,
+    #ffffff 70%
+  );
+  background-size: 200% auto;
+  color: transparent;
+  -webkit-background-clip: text;
+  background-clip: text;
+  animation: luxuryShimmer 6s linear infinite;
+}
+
+@keyframes luxuryShimmer {
+  to {
+    background-position: 200% center;
+  }
+}
+
 .brand-tagline {
   font-family: "Inter", sans-serif;
   font-size: 10px;
@@ -169,6 +199,10 @@ section {
   color: var(--champagne-deep);
 }
 
+.header .brand-tagline {
+  color: var(--champagne);
+}
+
 .header-nav {
   display: flex;
   align-items: center;
@@ -176,13 +210,14 @@ section {
   gap: 8px;
   padding: 8px;
   border-radius: 999px;
-  background: rgba(255, 255, 255, 0.78);
-  border: 1px solid rgba(17, 34, 64, 0.08);
-  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.65);
+  background: rgba(255, 255, 255, 0.05);
+  border: 1px solid rgba(255, 255, 255, 0.1);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05);
+  backdrop-filter: blur(10px);
 }
 
 .header-nav a {
-  color: rgba(17, 34, 64, 0.72);
+  color: rgba(255, 255, 255, 0.8);
   text-decoration: none;
   font-size: 13px;
   letter-spacing: 1.6px;
@@ -201,19 +236,21 @@ section {
   height: 2px;
   background: linear-gradient(90deg, transparent, var(--champagne), transparent);
   opacity: 0;
-  transition: opacity 0.3s ease;
+  transform: scaleX(0);
+  transition: opacity 0.3s ease, transform 0.3s ease;
 }
 
 .header-nav a:hover,
 .header-nav a.is-active {
-  color: var(--emerald-deep);
-  background: rgba(17, 34, 64, 0.06);
-  transform: translateY(-1px);
+  color: var(--white);
+  background: transparent;
+  transform: translateY(-2px);
 }
 
 .header-nav a:hover::after,
 .header-nav a.is-active::after {
   opacity: 1;
+  transform: scaleX(1);
 }
 
 .header-actions {
@@ -231,16 +268,22 @@ section {
   min-width: 180px;
   padding: 12px 16px;
   border-radius: 18px;
-  background: linear-gradient(180deg, rgba(17, 34, 64, 0.06) 0%, rgba(17, 34, 64, 0.02) 100%);
-  border: 1px solid rgba(17, 34, 64, 0.1);
-  color: var(--emerald-deep);
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 100%);
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  color: var(--white);
+  transition: transform 0.3s ease, background 0.3s ease;
+}
+
+.header-contact-pill:hover {
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0.06) 100%);
+  transform: translateY(-2px);
 }
 
 .header-contact-pill span {
   font-size: 10px;
   letter-spacing: 2.6px;
   text-transform: uppercase;
-  color: rgba(17, 34, 64, 0.54);
+  color: rgba(255, 255, 255, 0.7);
 }
 
 .header-contact-pill strong {
@@ -275,21 +318,21 @@ section {
   width: 48px;
   height: 48px;
   border-radius: 16px;
-  border: 1px solid rgba(17, 34, 64, 0.12);
-  background: rgba(255, 255, 255, 0.82);
+  border: 1px solid rgba(255, 255, 255, 0.15);
+  background: rgba(255, 255, 255, 0.1);
   padding: 10px;
   cursor: pointer;
   gap: 4px;
   flex-direction: column;
   justify-content: center;
-  box-shadow: 0 10px 24px rgba(17, 34, 64, 0.08);
+  box-shadow: 0 10px 24px rgba(0, 0, 0, 0.15);
 }
 
 .menu-toggle span {
   display: block;
   height: 2px;
   width: 100%;
-  background: var(--emerald-deep);
+  background: var(--white);
   border-radius: 2px;
 }
 
@@ -812,52 +855,185 @@ section {
