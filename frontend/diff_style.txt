diff --git a/style.css b/style.css
index ecb0b51..bf852d6 100644
--- a/style.css
+++ b/style.css
@@ -86,23 +86,23 @@ section {
   position: sticky;
   top: 0;
   z-index: 100;
-  background: rgba(247, 243, 234, 0.82);
+  background: rgba(10, 21, 38, 0.95);
   backdrop-filter: blur(18px);
   padding: 16px 30px;
   display: flex;
   justify-content: space-between;
   align-items: center;
   gap: 20px;
-  transition: all 0.35s ease;
-  box-shadow: 0 14px 32px rgba(17, 34, 64, 0.08);
-  border-bottom: 1px solid rgba(17, 34, 64, 0.08);
+  transition: all 0.35s ease, background 0.3s ease, border-color 0.3s ease;
+  box-shadow: 0 14px 32px rgba(0, 0, 0, 0.2);
+  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
 }
 
 .header.shrink {
   padding-top: 12px;
   padding-bottom: 12px;
-  background: rgba(247, 243, 234, 0.94);
-  box-shadow: 0 18px 36px rgba(17, 34, 64, 0.12);
+  background: rgba(10, 21, 38, 0.98);
+  box-shadow: 0 18px 36px rgba(0, 0, 0, 0.25);
 }
 
 .logo {
@@ -111,6 +111,12 @@ section {
   gap: 14px;
   text-decoration: none;
   flex-shrink: 0;
+  transition: transform 0.3s ease;
+}
+
+.logo:hover {
+  transform: translateY(-2px);
+  filter: drop-shadow(0 4px 12px rgba(195, 163, 98, 0.3));
 }
 
 .logo img {
@@ -151,6 +157,10 @@ section {
   color: rgba(17, 34, 64, 0.56);
 }
 
+.header .brand-eyebrow {
+  color: rgba(255, 255, 255, 0.6);
+}
+
 .brand-name {
   font-family: "Playfair Display", serif;
   font-size: 25px;
@@ -160,6 +170,26 @@ section {
   color: var(--emerald-deep);
 }
 
+.header .brand-name {
+  background: linear-gradient(
+    110deg,
+    #ffffff 30%,
+    var(--champagne) 50%,
+    #ffffff 70%
+  );
+  background-size: 200% auto;
+  color: transparent;
+  -webkit-background-clip: text;
+  background-clip: text;
+  animation: luxuryShimmer 6s linear infinite;
+}
+
+@keyframes luxuryShimmer {
+  to {
+    background-position: 200% center;
+  }
+}
+
 .brand-tagline {
   font-family: "Inter", sans-serif;
   font-size: 10px;
@@ -169,6 +199,10 @@ section {
   color: var(--champagne-deep);
 }
 
+.header .brand-tagline {
+  color: var(--champagne);
+}
+
 .header-nav {
   display: flex;
   align-items: center;
@@ -176,13 +210,14 @@ section {
   gap: 8px;
   padding: 8px;
   border-radius: 999px;
-  background: rgba(255, 255, 255, 0.78);
-  border: 1px solid rgba(17, 34, 64, 0.08);
-  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.65);
+  background: rgba(255, 255, 255, 0.05);
+  border: 1px solid rgba(255, 255, 255, 0.1);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05);
+  backdrop-filter: blur(10px);
 }
 
 .header-nav a {
-  color: rgba(17, 34, 64, 0.72);
+  color: rgba(255, 255, 255, 0.8);
   text-decoration: none;
   font-size: 13px;
   letter-spacing: 1.6px;
@@ -201,19 +236,21 @@ section {
   height: 2px;
   background: linear-gradient(90deg, transparent, var(--champagne), transparent);
   opacity: 0;
-  transition: opacity 0.3s ease;
+  transform: scaleX(0);
+  transition: opacity 0.3s ease, transform 0.3s ease;
 }
 
 .header-nav a:hover,
 .header-nav a.is-active {
-  color: var(--emerald-deep);
-  background: rgba(17, 34, 64, 0.06);
-  transform: translateY(-1px);
+  color: var(--white);
+  background: transparent;
+  transform: translateY(-2px);
 }
 
 .header-nav a:hover::after,
 .header-nav a.is-active::after {
   opacity: 1;
+  transform: scaleX(1);
 }
 
 .header-actions {
@@ -231,16 +268,22 @@ section {
   min-width: 180px;
   padding: 12px 16px;
   border-radius: 18px;
-  background: linear-gradient(180deg, rgba(17, 34, 64, 0.06) 0%, rgba(17, 34, 64, 0.02) 100%);
-  border: 1px solid rgba(17, 34, 64, 0.1);
-  color: var(--emerald-deep);
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 100%);
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  color: var(--white);
+  transition: transform 0.3s ease, background 0.3s ease;
+}
+
+.header-contact-pill:hover {
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0.06) 100%);
+  transform: translateY(-2px);
 }
 
 .header-contact-pill span {
   font-size: 10px;
   letter-spacing: 2.6px;
   text-transform: uppercase;
-  color: rgba(17, 34, 64, 0.54);
+  color: rgba(255, 255, 255, 0.7);
 }
 
 .header-contact-pill strong {
@@ -275,21 +318,21 @@ section {
   width: 48px;
   height: 48px;
   border-radius: 16px;
-  border: 1px solid rgba(17, 34, 64, 0.12);
-  background: rgba(255, 255, 255, 0.82);
+  border: 1px solid rgba(255, 255, 255, 0.15);
+  background: rgba(255, 255, 255, 0.1);
   padding: 10px;
   cursor: pointer;
   gap: 4px;
   flex-direction: column;
   justify-content: center;
-  box-shadow: 0 10px 24px rgba(17, 34, 64, 0.08);
+  box-shadow: 0 10px 24px rgba(0, 0, 0, 0.15);
 }
 
 .menu-toggle span {
   display: block;
   height: 2px;
   width: 100%;
-  background: var(--emerald-deep);
+  background: var(--white);
   border-radius: 2px;
 }
 
@@ -812,52 +855,185 @@ section {
   box-sizing: border-box;
 }
 
-.property-card-inner {
-  background: var(--white);
+.card.property-card-link {
+  position: relative;
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  width: 100%;
+  height: 380px; 
+  background: var(--emerald-deep);
   border-radius: 12px;
   overflow: hidden;
+  text-decoration: none;
   box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
   border: 1px solid rgba(0, 0, 0, 0.05);
-  transition: transform 0.4s ease, box-shadow 0.4s ease;
 }
 
-.property-card-inner:hover {
-  transform: translateY(-6px);
-  box-shadow: 0 10px 30px rgba(0,0,0,0.1);
+.card.property-card-link::before {
+  content: '';
+  position: absolute;
+  top: 0;
+  left: 0;
+  width: 100%;
+  height: 100%;
+  background: linear-gradient(315deg, var(--emerald), var(--champagne));
+  opacity: 0;
+  transition: opacity 0.5s ease;
+  z-index: 0;
 }
 
-.property-card img {
+.card.property-card-link::after {
+  content: '';
+  position: absolute;
+  top: 0;
+  left: 0;
   width: 100%;
-  height: 260px;
-  object-fit: contain;
-  object-position: center;
-  background: #111;
-  display: block;
+  height: 100%;
+  background: linear-gradient(315deg, var(--emerald), var(--champagne));
+  filter: blur(30px);
+  opacity: 0;
+  transition: opacity 0.5s ease;
+  z-index: 0;
 }
 
-.property-card .content {
-  background: var(--porcelain);
-  padding: 22px;
+.card.property-card-link:hover::before,
+.card.property-card-link:hover::after {
+  opacity: 1;
 }
 
-.property-card .content a {
-  display: inline-block;
-  margin-top: 12px;
-  padding: 10px 20px;
-  background: var(--emerald);
+.card.property-card-link b {
+  position: absolute;
+  inset: 6px;
+  background: var(--white);
+  border-radius: 8px;
+  z-index: 2;
+  transition: background 0.5s ease;
+}
+
+.card.property-card-link:hover b {
+  background: rgba(10, 21, 38, 0.85); /* Dark background on hover so text pops */
+}
+
+.card.property-card-link img {
+  position: absolute;
+  z-index: 3;
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  scale: 0.8;
+  opacity: 1;
+  transition: 0.5s cubic-bezier(0.25, 1, 0.5, 1);
+  border-radius: 8px;
+}
+
+.card.property-card-link:hover img {
+  scale: 0.5;
+  opacity: 0.25;
+  transform: translateY(-50px);
+}
+
+.card-badge {
+  position: absolute;
+  top: 20px;
+  left: 20px;
+  z-index: 4;
+  padding: 6px 14px;
+  border-radius: 999px;
+  font-size: 11px;
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.5px;
+  box-shadow: 0 4px 10px rgba(0,0,0,0.15);
+}
+
+.card-badge.completed,
+.card-badge.available {
+  background: #2e5a4a;
+  color: #ffffff;
+}
+
+.card-badge.ongoing,
+.card-badge.under {
+  background: var(--champagne);
+  color: var(--emerald-deep);
+}
+
+.card.property-card-link .content {
+  position: absolute;
+  z-index: 3;
+  bottom: 0px;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  width: 100%;
+  padding: 0 20px;
+  transform: scale(0);
+  opacity: 0;
+  transition: 0.5s cubic-bezier(0.25, 1, 0.5, 1);
+}
+
+.card.property-card-link:hover .content {
+  transform: scale(1);
+  opacity: 1;
+  bottom: 30px;
+}
+
+.content .title {
+  position: relative;
   color: var(--white);
-  text-decoration: none;
-  font-size: 14px;
+  font-family: "Playfair Display", serif;
   font-weight: 600;
-  border-radius: 4px;
+  line-height: 1.3em;
+  font-size: 24px;
+  letter-spacing: 1px;
+  text-align: center;
+  margin-bottom: 8px;
+}
+
+.content .title span {
+  font-family: "Inter", sans-serif;
+  font-weight: 400;
+  font-size: 14px;
+  color: var(--champagne);
   letter-spacing: 0.5px;
-  transition: all 0.3s ease;
+  text-transform: none;
+}
+
+.card-specs {
+  color: rgba(255, 255, 255, 0.9);
+  font-size: 14px;
+  margin-bottom: 16px;
+  letter-spacing: 0.5px;
+}
+
+.card-actions {
+  display: flex;
+  gap: 8px;
+  margin-bottom: 20px;
+}
+
+.action-tag {
+  font-size: 11px;
+  padding: 6px 14px;
+  border-radius: 999px;
+  background: rgba(255, 255, 255, 0.1);
+  color: var(--white);
+  border: 1px solid rgba(255, 255, 255, 0.2);
+  text-transform: uppercase;
+  letter-spacing: 1px;
 }
 
-.property-card .content a:hover {
+.card-cta {
+  font-size: 13px;
+  font-weight: 600;
+  letter-spacing: 1.5px;
+  color: var(--emerald-deep);
   background: var(--champagne);
-  transform: translateY(-2px);
-  box-shadow: 0 8px 20px rgba(189, 160, 105, 0.25);
+  padding: 10px 24px;
+  border-radius: 4px;
+  text-transform: uppercase;
+  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
 }
 
 .slide-btn {
@@ -1347,10 +1523,24 @@ section {
 }
 
 .amenity-icon-style {
-  font-size: 28px;
-  color: var(--emerald);
+  font-size: 24px;
+  color: var(--emerald-deep);
   margin-bottom: 16px;
-  display: block;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  width: 60px;
+  height: 60px;
+  background-color: rgba(195, 163, 98, 0.15); /* Soft beige background */
+  border-radius: 50%;
+  transition: all 0.3s ease;
+}
+
+.amenity-card:hover .amenity-icon-style {
+  background-color: rgba(195, 163, 98, 0.25);
+  transform: scale(1.05); /* Slight lift */
+  color: var(--emerald-deep);
+  box-shadow: 0 4px 15px rgba(195, 163, 98, 0.2); /* Subtle glow */
 }
 
 .amenity-name {
